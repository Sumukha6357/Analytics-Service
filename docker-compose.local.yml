services:
  postgres:
    ports:
      - "${POSTGRES_PORT}:5432"

  redis:
    ports:
      - "${REDIS_PORT}:6379"

  api:
    ports:
      - "${API_PORT}:${API_PORT}"

  web:
    ports:
      - "${WEB_PORT}:${WEB_PORT}"

